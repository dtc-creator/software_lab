name: JPRO Interactive RDP
on:
  workflow_dispatch:
    inputs:
      minutes:
        description: "Keep the RDP session open this many minutes (max 340)"
        default: "180"
permissions:
  contents: write
jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 350
    steps:
      - name: Enable RDP + set admin password
        shell: pwsh
        run: |
          net user runneradmin "${{ secrets.RDP_PASSWORD }}"
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name fDenyTSConnections -Value 0
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name UserAuthentication -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      - name: Prep bore + start JPRO install (background)
        shell: pwsh
        run: |
          cargo install bore-cli 2>&1 | Select-Object -Last 2
          Invoke-WebRequest "https://s3.amazonaws.com/JPROUpdate/JPRO+Fleet+Diagnostics/JproFullSetup.exe" -OutFile C:\setup.exe
          Start-Process C:\setup.exe -ArgumentList '/quiet','/norestart'
          New-Item -ItemType Directory -Force C:\jpro_drop | Out-Null

      - name: Start tunnel + write address back to repo
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $bore = "$env:USERPROFILE\.cargo\bin\bore.exe"
          Start-Process $bore -ArgumentList 'local','3389','--to','bore.pub' -RedirectStandardOutput C:\bore.out -RedirectStandardError C:\bore.err -NoNewWindow
          Start-Sleep 12
          $line = (Get-Content C:\bore.out,C:\bore.err -EA SilentlyContinue | Select-String 'bore.pub:\d+' | Select-Object -First 1)
          if($line -match 'bore\.pub:(\d+)'){ $addr = "bore.pub:" + $Matches[1] } else { $addr = "TUNNEL-FAILED: $line" }
          Write-Host "ADDRESS: $addr"
          # write to repo file rdp_address.txt via API (handle existing sha)
          $repo = $env:GITHUB_REPOSITORY
          $hdr = @{ Authorization = "token $env:GH_TOKEN"; "User-Agent"="runner"; Accept="application/vnd.github+json" }
          $sha = $null
          try { $sha = (Invoke-RestMethod -Headers $hdr -Uri "https://api.github.com/repos/$repo/contents/rdp_address.txt").sha } catch {}
          $b64 = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($addr + "  (user: runneradmin)"))
          $body = @{ message="rdp address"; content=$b64 } ; if($sha){ $body.sha=$sha }
          Invoke-RestMethod -Method PUT -Headers $hdr -Uri "https://api.github.com/repos/$repo/contents/rdp_address.txt" -Body ($body|ConvertTo-Json) | Out-Null
          Write-Host "address committed to repo: rdp_address.txt"

      - name: Hold session open
        shell: pwsh
        run: |
          $mins=[int]"${{ inputs.minutes }}"
          for($i=0;$i -lt $mins;$i++){ Start-Sleep 60; if($i % 10 -eq 0){ Write-Host "alive $i/$mins" } }

      - name: Collect screenshots
        if: always()
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force shots | Out-Null
          Copy-Item C:\jpro_drop\* shots\ -Recurse -EA SilentlyContinue
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jpro-rdp-shots
          path: shots
