name: JPRO Interactive RDP
on:
  workflow_dispatch:
    inputs:
      minutes:
        description: "Keep the RDP session open this many minutes (max 340)"
        default: "180"

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 350
    steps:
      - name: Enable RDP + set admin password
        shell: pwsh
        run: |
          net user runneradmin "${{ secrets.RDP_PASSWORD }}"
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name fDenyTSConnections -Value 0
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name UserAuthentication -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Write-Host "RDP enabled (user: runneradmin)"

      - name: Prep bore + start JPRO install (background)
        shell: pwsh
        run: |
          cargo install bore-cli 2>&1 | Select-Object -Last 2
          Invoke-WebRequest "https://s3.amazonaws.com/JPROUpdate/JPRO+Fleet+Diagnostics/JproFullSetup.exe" -OutFile C:\setup.exe
          Start-Process C:\setup.exe -ArgumentList '/quiet','/norestart'
          New-Item -ItemType Directory -Force C:\jpro_drop | Out-Null
          Write-Host "JPRO installing in background; save screenshots to C:\jpro_drop"

      - name: Start tunnel + PRINT ADDRESS (this step completes)
        shell: pwsh
        run: |
          $bore = "$env:USERPROFILE\.cargo\bin\bore.exe"
          Start-Process $bore -ArgumentList 'local','3389','--to','bore.pub' -RedirectStandardOutput C:\bore.out -RedirectStandardError C:\bore.err -NoNewWindow
          Start-Sleep 12
          $addr = (Get-Content C:\bore.out,C:\bore.err -EA SilentlyContinue | Select-String 'bore.pub:\d+' | Select-Object -First 1)
          Write-Host "RAW: $addr"
          if($addr -match 'bore\.pub:(\d+)'){
            $hostport = "bore.pub:" + $Matches[1]
            Write-Host "::notice title=RDP ADDRESS::$hostport  user=runneradmin"
            Write-Host "CONNECT_RDP=$hostport"
          } else { Write-Host "tunnel address not found yet"; Get-Content C:\bore.out,C:\bore.err -EA SilentlyContinue | Write-Host }

      - name: Hold session open
        shell: pwsh
        run: |
          $mins=[int]"${{ inputs.minutes }}"
          for($i=0;$i -lt $mins;$i++){ Start-Sleep 60; if($i % 10 -eq 0){ Write-Host "session alive: $i / $mins min" } }

      - name: Collect screenshots
        if: always()
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force shots | Out-Null
          Copy-Item C:\jpro_drop\* shots\ -Recurse -EA SilentlyContinue
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jpro-rdp-shots
          path: shots
